import { SampleReportDemo } from "@/components/report/SampleReportDemo";

/** Спринт 1: тот же контент, что `/report/sample`; позже — данные по scan id из БД. */
export default function ReportByScanIdPage() {
  return <SampleReportDemo />;
}
